# ── Core LLM & Agents ──────────────────────────────────────────────────────────
langchain==0.2.16
langchain-community==0.2.16
langchain-aws==0.1.17
langgraph==0.2.28

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.32
psycopg2-binary==2.9.9

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb==0.5.5

# ── Document Processing ───────────────────────────────────────────────────────
pypdf==4.3.1
pdfplumber==0.11.4
unstructured==0.15.0

# ── Vision / VLM ──────────────────────────────────────────────────────────────
# pymupdf rasterizes scanned PDF pages to images for the VLM-OCR ingestion
# rescue (src/ingestion/vlm_ocr.py). No new LLM SDK needed for the vision
# client itself (src/agents/vlm_client.py) — dev reuses langchain-groq
# (already above) against Groq's vision-capable model, and prod reuses
# langchain-aws (already above) since Bedrock Claude is natively multimodal.
pymupdf==1.24.9
Pillow==10.4.0                   # renders synthetic invoice/PO test images (data/synthetic/generate_invoice_images.py)

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.0.1
boto3==1.34.160                  # for Bedrock + S3 + Lambda

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas==0.2.6
langchain-openai==0.1.20
datasets==2.21.0

# ── API & UI ──────────────────────────────────────────────────────────────────
fastapi==0.112.2
uvicorn[standard]==0.30.6
streamlit==1.37.1
pydantic==2.8.2

# ── Data Generation ───────────────────────────────────────────────────────────
faker==26.0.0
pandas==2.2.2
numpy==1.26.4

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
tenacity==8.5.0
loguru==0.7.2
httpx==0.27.0
tqdm==4.66.5

# ── Groq API run locally ──────────────────────────────────────────────────────
langchain-groq==0.1.9